<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script>
        const ROOM_ID = "<%= roomId %>"
    </script>
    <script src="peerjs.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <title>Portavita SMC</title>
    <style>
        body {
            margin: 0;
            background-color: silver;
        }
        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, 300px);
            grid-auto-rows: 300px;
            gap: 20px;
            margin: 20px;
        }
        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border: 3px solid white;
            border-radius: 10px;
        }
        header {
            left: 0;
            right: 0;
            height: 50px;
            top: 0;
            background-color: #3A84AA;
            border-bottom: 1px solid black;
        }
        #logo {
            position: fixed;
            left: -4px;
            padding: 1px;
            height: 50px;
        }
        #title {
            position: fixed;
            display: inline-block;
            padding-bottom: 10px;
            font-family: sans-serif;
            font-size: 17px;
            margin-top: 2px;
            color: white;
            top: 7px;
            right: 10px;
            line-height: 17px;
            text-align: right;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <header>
        <img id="logo" src="logo_single_colour_no_tag.svg" alt="Portavita Networked Health">
        <div id="title">Portavita VC<br><span style="font-size: 12px;color:#00577B;font-weight:bold">secure medical video conferences</span></div>
    </header>
    <div id="video-grid"></div>
</body>
</html>
